# Exercise 04 — Uppercase a String

Ask the user for input, then print it in uppercase.

## Requirements
- Use `ctype.h` (`toupper`)
- Keep the original string unchanged (just print uppercase)

## Run
```bash
clang -std=c11 -Wall -Wextra -Werror uppercase.c -o uppercase
./uppercase
```
