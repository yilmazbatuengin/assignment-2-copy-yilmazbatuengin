# Exercise 03 — String Length (Without `strlen`)

Write a function `string_length` that counts characters until `\0`.

## Requirements
- Do **not** use `strlen`
- Use a loop (`while` or `for`)
- Return the length as an `int`

## Run
```bash
clang -std=c11 -Wall -Wextra -Werror length.c -o length
./length
```
