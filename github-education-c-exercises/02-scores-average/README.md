# Exercise 02 — Average of Scores (Arrays + Loops)

Write a program that:
1. Asks the user for **N** scores (integers)
2. Prints the **average** as a floating point number with 2 decimals

## Requirements
- Use an array
- Use a loop
- Use a helper function `average`

## Run
```bash
clang -std=c11 -Wall -Wextra -Werror scores.c -o scores
./scores
```
